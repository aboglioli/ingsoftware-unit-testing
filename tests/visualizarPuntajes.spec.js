'use strict'
const chai = require('chai');
const expect = chai.expect;
const tutor = require('../src/tutor');

describe('Tutor', () => {
  it('getPuntaje() El tutor deberia visualizar el historial de puntajes de studiantes a cargo', () => {
let tutor1 = new Tutor('Nombre', 'Apellido', '08/01/1970');

    expect(tutor1.getPuntaje(pers, 'admin'));
  });




 })

